name: filc

on:
  [ push, pull_request ]

jobs:
  test-options:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: ls
      - name: run script test_options
        run: python3 filc/test/test_options.py
